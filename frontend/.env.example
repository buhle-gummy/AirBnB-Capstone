# Leave unset for local Vite development; /api is proxied to http://localhost:5000.
# For a deployed frontend, set this to the public origin of the deployed backend.
VITE_API_URL=https://your-deployed-backend.example.com
